# 工作流9：固定账单模式

当用户提到每月/每周固定支出时（房租、订阅服务等）：

1. **记录固定账单模板**：运行 `add_recurring`
2. **月初提醒**：当用户在每月初与教练对话时，主动运行 `get_recurring` 获取待扣固定账单，提醒用户：
   "老大，这个月的固定账单有：房租 ￥3000、Spotify ￥15...要我帮你一起记上吗？"
3. **用户确认后**：批量调用 `add_bill` 记录，情绪自动标记为"日常"
4. **停用固定账单**：运行 `delete_recurring`
